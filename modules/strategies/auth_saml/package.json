{
    "name": "@oauth-server/auth-saml",
    "version": "0.0.1",
    "description": "SAML 2.0 authentication strategy for OAuth Server",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "node esbuild.config.mjs",
        "build:types": "tsc --emitDeclarationOnly",
        "clean": "rm -rf dist",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@oauth-server/shared": "file:../../shared",
        "xml-crypto": "^6.1.2"
    },
    "devDependencies": {
        "@aws-sdk/client-dynamodb": "^3.0.0",
        "@aws-sdk/lib-dynamodb": "^3.0.0",
        "@types/aws-lambda": "^8.10.0",
        "@types/node": "^20.0.0",
        "@types/xml-crypto": "^1.4.6",
        "esbuild": "^0.24.0",
        "typescript": "^5.0.0"
    },
    "engines": {
        "node": ">=20.0.0"
    },
    "license": "MIT"
}
